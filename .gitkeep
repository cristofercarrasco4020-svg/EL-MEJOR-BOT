# Entra a la carpeta de tu bot
cd ~/EL-MEJOR-BOT

# Crear el archivo en la carpeta principal de database
touch config/database/.gitkeep

# Crear archivos en las carpetas de datos
mkdir -p config/database/economy
touch config/database/economy/.gitkeep

mkdir -p config/database/gacha
touch config/database/gacha/.gitkeep

mkdir -p config/database/profile
touch config/database/profile/.gitkeep
